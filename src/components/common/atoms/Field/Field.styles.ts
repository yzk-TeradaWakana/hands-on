import { css } from "@emotion/react";

/** applied style */
export const styles = {
  base: css({
    /** base style */
    fontFamily: [
      'Roboto',
      '"Noto Sans JP"',
      '"Helvetica"',
      'Arial',
      'sans-serif',
    ].join(',')
  }),
}