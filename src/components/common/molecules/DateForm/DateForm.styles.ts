import { css } from "@emotion/react";

/** applied style */
export const styles = {
  base: css({
    /** base style */
  }),
  primary: css({
    /** カラー：プライマリー */
    color: '#2196f3',
  }),
  secondary: css({
    /** カラー：セカンダリー */
    color: '#e91e63',
  }),
  tertiary: css({
    /** カラー：ターシャリー */
    color: '#009688',
  }),
  error: css({
    /** カラー：エラー */
    color: '#f44336',
  }),
  /* Add styles here */
}