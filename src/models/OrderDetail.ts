export type OrderDetail = {
  deliveryDate: string,
  quantity: number,
  delivery: string,
  factory: string,
  manufacturing: string,
  status: string,
  backorder: string,
  note: string,
  updateReason: string,
  updateData: string
};